import { Explainer } from '../../../src/Query/Explain/Explainer';

describe('Explainer', () => {
    it('construct', () => {
        // @ts-expect-error explainer unused
        const explainer = new Explainer();
    });
});
