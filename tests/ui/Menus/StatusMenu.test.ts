import { StatusMenu } from '../../../src/ui/Menus/StatusMenu';

export {};

describe('StatusMenu', () => {
    it('creation', () => {
        // @ts-expect-error unused
        const menu = new StatusMenu();
    });
});
